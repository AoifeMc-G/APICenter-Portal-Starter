{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "location": {
            "value": "${AZURE_LOCATION}"
        },
        "apiCenterExisted": {
            "value": "${USE_EXISTING_API_CENTER=true}"
        },
        "apiCenterName": {
            "value": "${AZURE_API_CENTER}"
        },
        "apiCenterResourceGroupName": {
            "value": "${AZURE_RESOURCE_GROUP_NAME}"
        },
        "resourceGroupName": {
            "value": "${AZURE_RESOURCE_GROUP_NAME}"
        },
        "useMonitoring": {
            "value": "${USE_MONITORING=false}"
        },
        "staticAppLocation": {
            "value": "${AZURE_STATIC_APP_LOCATION=westeurope}"
        },
        "staticAppName": {
            "value": "${AZURE_STATIC_APP_NAME}"
        }
    }
}